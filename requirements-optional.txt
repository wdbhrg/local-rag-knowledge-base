# Optional: semantic chunking, chunk_vectors (hybrid retrieval), Cross-Encoder rerank, RAG evaluation
# pip install -r requirements-optional.txt
# First run after install: rebuild KB so chunk_vectors is populated (see README).

# Core ML libraries
sentence-transformers>=3.0.0
torch
numpy>=1.24.0
scikit-learn>=1.3.0

# RAG evaluation
ragas>=0.2.0

# Optional tracing / experiment UI (not required for eval/evaluate_rag.py): arize-phoenix
langchain-openai>=0.2.0
openai>=1.0.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Performance optimization
# faiss-cpu  # Uncomment for large-scale vector search (10k+ vectors)
# diskcache  # Uncomment for persistent query caching
